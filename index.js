import {
    getLocationHash,
    genCID,
    create_DID,
    hash,
    register_DID,
    resolve_DID,
} from './main';

export { getLocationHash, genCID, create_DID, hash, register_DID, resolve_DID };
