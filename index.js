import {
    getLocationHash,
    genCID,
    create_DID,
    hash,
    register_DID,
    resolve_DID,
} from './main';

module.exports = {
    getLocationHash,
    genCID,
    create_DID,
    hash,
    register_DID,
    resolve_DID,
};
